<!--pages/dataTransfer/dataTransfer.wxml-->
<view class="box" bind:tap="_itemSelect">
  <view wx:for="{{foodTable}}" wx:key="_id">
    <FoodItem food-item="{{item}}" id="{{item._id}}" data-text="{{item.title}}"/>
  </view>
</view>
